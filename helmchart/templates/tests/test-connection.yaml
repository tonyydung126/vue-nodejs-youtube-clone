apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tvtien.fullname" . }}-test-connection"
  labels:
    {{- include "tvtien.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tvtien.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
